SELECT * FROM `penjualan_detail`-> Digunakan untuk menampilkan seluruh isi kolom yang ada di tabel penjulan detail
SELECT id_obat, jumlah_obat, harga_obat FROM `penjualan_detail` -> Diguankan untuk menampilkan beberapa kolom di tabel penjulan_detail
seperti id_obat, jumlah_obat, harga_obat.
UPDATE penjualan_detail set harga_obat="2500", total_harga="2500" WHERE id_obat=2; -> digunakan untuk update field harga_obat & total_harga yang sebelumnya 
"2000" menjadi "2500" yang memilki id_obat =2;
DELETE FROM `penjualan_detail` WHERE id_obat=1 -> Digunakan untuk delete data/record dalam 1 baris yang ada di tabel penjulan_detail yang memiliki id_obat=1
SELECT * FROM penjualan_detail WHERE id_transaksi_detail=1 OR id_transaksi=1 - > Digunakan untuk mengambil data id_transaksi_detail 1 & id_transaksi= 1
SELECT * FROM penjualan_detail ORDER BY id_transaksi_detail DESC - > Digunakan untuk menampilkan data seluruh penjulan_detail yang di urutkan berdasarkan kolom id_transaksi mulai dari terbesar
SELECT * FROM penjualan_detail ORDER BY id_transaksi_detail ASC -> Digunakan untuk menampilkan data seluruh penjulana_detail yang di urutkan berdasarkan kolom id_transaksi mulai dari terkecil.
SELECT * FROM penjualan_detail ORDER BY id_obat LIMIT 5 -> Digunakan untuk menampilkan data yang di urutkan berdasarkan id_obat 
urutan yang paling atas dengan batasan 5 record data yang di tampilkan(* Jika record data id_obat 1 tidak ada maka akan di tampilkan data angka record setelahnya/selanjutnya*)
SELECT * FROM penjualan_detail LIMIT 4,5 -> Digunakan untuk menampilkan data 5 record data setelah row ke 4,(yang di tampilkan setelah record data row 4)
SELECT * FROM penjualan_detail ORDER BY id_obat ASC LIMIT 4,5 -> Digunakan untuk menampilkan 5 record data setelah row ke 4, secara berurutan dari terkecil hingga id_obat terbesar
SELECT * FROM penjualan_detail WHERE id_obat > 5 - > Diguanakan untuk menampilkan record data yang di pilih id_obat yang lebih dari id_obat 5 yang mau di tampilkan (* Jadi tidak ada id_obat yang di bawah 5*)
SELECT * FROM penjualan_detail WHERE id_obat >= 5 - > Digunakan untuk menampilkan record data yang di pilih id_obat yang sama dengan id_obat 5 yang mau di tampilkan (* Jadi id_obat yang lebih dari sama dengan 5 akan di tampilkan*)
